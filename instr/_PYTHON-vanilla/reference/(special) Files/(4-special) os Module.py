## os Module
import os       # load the os module into memory


## Library Functions (Black Boxes)
# remove Function
os.remove('sample.txt')                # deletes a file on the disk (arg: str)

# rename Function
os.rename('sample.txt', 'intro.txt')   # renames a file (arg1: str, arg2: str)
